# 低空空管自动化系统 — 移动端测试启动指南

> 适用环境：Windows 10/11 + Android 真机  
> 当前测试模式：浏览器模式（手机 Chrome 打开 H5 页面）  
> 更新日期：2026-08-04

---

## 一、当前环境状态

| 项目 | 状态 | 详情 |
|------|------|------|
| Python 3.13 | ✅ 就绪 | 虚拟环境 `.venv` 位于 `mobile-test-framework/` |
| Appium 3.6.0 | ✅ 就绪 | UiAutomator2 驱动 8.2.2 |
| ADB 37.0.1 | ✅ 就绪 | 项目内置 `platform-tools/adb.exe` |
| ANDROID_HOME | ✅ 就绪 | 指向 `D:\liyang\code\Android_Auto_Test` |
| 测试设备 | ✅ 已连接 | 24117RK2CC, Android 16, 分辨率 1440×3200 |
| 手机 Chrome | ✅ 已安装 | Chrome 150.0.7871 |
| ChromeDriver | ✅ 已安装 | 150.0.7871.124, 位于 `~/.appium/chromedriver/` |
| APK | ⏳ 待开发 | 当前无 APK，通过浏览器测试 H5 页面 |

---

## 二、首次环境搭建（新设备部署）

以下步骤在当前设备上**已完成**，新设备部署时按顺序执行：

### 2.1 设置系统环境变量

```
1. Win + R → 输入 sysdm.cpl → 回车
2. 高级 → 环境变量
3. 用户变量 → 新建：
   变量名：ANDROID_HOME
   变量值：D:\liyang\code\Android_Auto_Test
4. 确定 → 确定 → 重启所有终端
```

### 2.2 创建 Python 虚拟环境

```powershell
cd D:\liyang\code\Android_Auto_Test\mobile-test-framework
python -m venv .venv
.venv\Scripts\Activate.ps1
pip install -r requirements.txt
```

### 2.3 安装 Appium 和驱动

```powershell
npm install -g appium
appium driver install uiautomator2
```

### 2.4 手机端配置

1. 设置 → 关于手机 → 连续点击版本号 7 次 → 开启开发者模式
2. 设置 → 开发人员选项 → 开启 USB 调试
3. Google Play Store → 安装 Google Chrome
4. USB 数据线连接电脑 → 手机上点击"允许 USB 调试"

### 2.5 下载 ChromeDriver

手机 Chrome 版本与 ChromeDriver 必须匹配。查看手机 Chrome 版本：

```powershell
adb shell dumpsys package com.android.chrome | findstr versionName
```

根据版本号到以下地址下载对应的 ChromeDriver：
```
https://storage.googleapis.com/chrome-for-testing-public/
```

下载后解压，将 `chromedriver.exe` 放到：
```
C:\Users\<你的用户名>\.appium\chromedriver\<版本号>\
```

比如 Chrome 150.0.7871 对应：
```
C:\Users\keda\.appium\chromedriver\150.0.7871.124\chromedriver.exe
```

### 2.6 验证环境

```powershell
cd D:\liyang\code\Android_Auto_Test\mobile-test-framework
.venv\Scripts\Activate.ps1
python setup_check.py --fix
```

10 项检查全部通过即表示环境就绪。

---

## 三、日常启动步骤

### 步骤 1：启动 Appium Server

打开一个 PowerShell 终端：

```powershell
appium --relaxed-security
```

看到以下输出表示启动成功：
```
[Appium] Welcome to Appium v3.6.0
[Appium] Appium REST http interface listener started on http://0.0.0.0:4723
```

> 注意：`--relaxed-security` 参数是必须的，用于解决 Android 14+ 的权限限制问题。

### 步骤 2：进入交互式测试模式

打开另一个 PowerShell 终端：

```powershell
cd D:\liyang\code\Android_Auto_Test\mobile-test-framework
.venv\Scripts\Activate.ps1
python -i manual_test.py
```

看到以下输出表示成功：
```
[OK] 配置已加载 (环境: test)
[OK] ADB已就绪 (在线设备: 1)
[OK] Appium Session已建立
[OK] Chrome已启动
```

### 步骤 3：开始测试操作

在 Python 交互提示符 `>>>` 后输入命令：

```python
# 打开测试页面
>>> launch()

# 查看页面信息
>>> driver.title
>>> driver.current_url

# 导入元素定位器
>>> from selenium.webdriver.common.by import By

# 查找页面元素
>>> driver.find_element(By.CSS_SELECTOR, "input[placeholder*='用户名']")
>>> driver.find_element(By.CSS_SELECTOR, ".btn-primary")

# 查找所有输入框
>>> inputs = driver.find_elements(By.CSS_SELECTOR, "input")
>>> for i in inputs: print(i.get_attribute("placeholder"))

# 输入文本
>>> el = driver.find_element(By.CSS_SELECTOR, "input[type='password']")
>>> el.send_keys("test123")

# 点击按钮
>>> driver.find_element(By.CSS_SELECTOR, "button").click()

# 页面滚动（手指从下往上）
>>> driver.swipe(500, 1500, 500, 500, 500)

# 截图
>>> screenshot("登录页面")

# 查看页面 HTML 源码
>>> print(driver.page_source[:500])

# 查看帮助
>>> help()

# 查看设备信息
>>> info()

# 退出
>>> exit()
```

---

## 四、运行自动化测试

### 快速命令

```powershell
cd D:\liyang\code\Android_Auto_Test\mobile-test-framework
.venv\Scripts\Activate.ps1

# 环境检查
python setup_check.py --fix

# 冒烟测试（5 个核心用例，约 2 分钟）
python run_tests.py --smoke

# 登录模块测试
python run_tests.py --module login

# 全部测试 + 生成 Allure 报告
python run_tests.py --report

# 详细输出模式
pytest tests/test_home.py -v --tb=long

# 排除耗时用例
pytest tests/ -m "not slow" -v
```

### 指定设备

```powershell
# 使用第 1 个设备（索引从 0 开始）
python run_tests.py --device 1
```

### 切换测试环境

```powershell
# 使用配置文件中的 dev 环境
$env:TEST_ENV = "dev"
python run_tests.py --smoke
```

---

## 五、切换测试模式

配置文件 `config/config.yaml` 中的 `test_mode` 控制测试模式：

### 浏览器模式 — 测试 HTML 原型（当前使用）

> `pages/` 目录包含 13 个 HTML 原型页面（手机端小程序 UI 模拟）。
> 通过本地 HTTP 服务器 + 手机 Chrome 浏览器进行测试。

```yaml
test_mode: browser
# 本地原型测试
test_url: "http://127.0.0.1:8080/小程序_首页.html"
# 远程H5测试
# test_url: "https://dkkgsit-test.testdolphin.com/atc/dashboard"
```

**完整工作流程：**

```powershell
# 终端1: 启动页面服务器 (8080端口 + adb reverse)
cd D:\liyang\code\Android_Auto_Test\mobile-test-framework
.venv\Scripts\Activate.ps1
python serve_pages.py

# 终端2: 启动 Appium Server
appium --relaxed-security

# 终端3: 进入交互测试模式
cd D:\liyang\code\Android_Auto_Test\mobile-test-framework
.venv\Scripts\Activate.ps1
python -i manual_test.py

# 在 Python 交互提示符中:
>>> launch()          # 导航到测试页面 (127.0.0.1:8080/小程序_首页.html)
>>> navigate("http://127.0.0.1:8080/小程序_登录.html")  # 切换页面
```

**工作原理：**
```
PC HTTP Server (:8080)  ←→  adb reverse  ←→  Phone (127.0.0.1:8080)
                                                      │
                                               Chrome browser
                                               accesses local pages
```

### 一键启动（自动化脚本）

在 `start.bat` 中选择 `[T] Test with prototypes`，一键启动：
1. 页面服务器 (后台)
2. Appium Server (后台)
3. 交互式测试控制台

### 原生 APP 模式（APK 就绪后切换）

```yaml
test_mode: native
```

**步骤1: 安装 APK 到设备**

```powershell
# 方式1: 用 ADB 直接安装
adb install -r "D:\path\to\app.apk"

# 方式2: 在交互模式中安装 (自动识别包名)
python -i manual_test.py
>>> install_apk("D:\path\to\app.apk")

# 方式3: 使用 start.bat 菜单 [I]
```

**步骤2: 获取正确的 appPackage 和 appActivity**

```powershell
# 方式1: 交互模式中查找 (推荐)
python -i manual_test.py
>>> find_app("dolphin")              # 搜索已安装的APP
>>> inspect_app("com.dolphin.atc")   # 获取详细信息

# 输出示例:
#   Main Activity: .MainActivity
#   All Activities:
#     - .MainActivity ← LAUNCHER
#     - .LoginActivity
#     - .HomeActivity
#   For config.yaml:
#     app_package: "com.dolphin.atc"
#     app_activity: ".MainActivity"

# 方式2: 手动用 ADB 命令
adb shell pm list packages | findstr dolphin       # 找包名
adb shell dumpsys package com.dolphin.atc | findstr "android.intent.action.MAIN" -A 5  # 找启动Activity
```

**步骤3: 更新配置文件**

```yaml
devices:
  - name: "24117RK2CC"
    platform_version: "16"
    app_package: "com.dolphin.atc"      # ← 从 inspect_app() 获取
    app_activity: ".MainActivity"       # ← 从 inspect_app() 获取
```

**步骤4: 验证**

```powershell
python -i manual_test.py
# 看到 "[OK] Appium Session已建立" 即表示成功
>>> info()   # 查看当前Activity
```

---

## 六、测试报告

```powershell
# 运行测试并生成 Allure 数据
pytest tests/ -v --alluredir=reports/allure-results

# 在浏览器中查看报告
allure serve reports/allure-results
```

---

## 七、故障排查

### Appium 端口被占用

```
[HTTP] Could not start REST http interface listener.
EADDRINUSE: address already in use 0.0.0.0:4723
```

**解决**：打开 PowerShell 运行：
```powershell
Get-NetTCPConnection -LocalPort 4723 | ForEach-Object { Stop-Process -Id $_.OwningProcess -Force }
```

### ChromeDriver 版本不匹配

```
No Chromedriver found that can automate Chrome '150.0.7871'
```

**解决**：下载匹配版本的 ChromeDriver，放到 `C:\Users\<用户名>\.appium\chromedriver\<版本号>\`

### 设备未找到

```
Could not find a connected Android device
```

**解决**：
```powershell
adb kill-server
adb start-server
adb devices
# 确保输出中有 "device" 状态
```

### 连接被拒绝

```
HTTPConnectionPool: Failed to establish a new connection: [WinError 10061]
```

**解决**：确认 Appium Server 在另一个终端中正在运行。

### ERR_CONNECTION_CLOSED

**解决**：检查手机网络是否正常，测试 URL 是否可访问。

### Activity class does not exist (原生模式)

```
Error: Activity class {com.dolphin.atc/com.dolphin.atc.MainActivity} does not exist.
```

**原因**：APP 未安装，或 `appPackage`/`appActivity` 配置错误。

**解决步骤**：
```powershell
# 1. 检查APP是否已安装
adb shell pm list packages | findstr dolphin

# 2. 如果未安装，先安装APK (见"原生APP模式"章节)
# 3. 如果已安装，获取正确的配置:
python -i manual_test.py
>>> find_app("dolphin")
>>> inspect_app("com.dolphin.atc")
# 4. 将输出的 app_package 和 app_activity 更新到 config.yaml
```

### 页面服务器无法访问 (connection refused)

手机 Chrome 访问 `http://127.0.0.1:8080/` 报错：

**解决**：
```powershell
# 确认 adb reverse 是否生效
adb reverse --list
# 应该输出: tcp:8080 tcp:8080

# 如果没有，手动建立:
adb reverse tcp:8080 tcp:8080

# 替代方案: 查看PC的局域网IP
ipconfig | findstr IPv4
# 手机访问: http://<PC_IP>:8080/
```

---

*文档版本：v2.0*  
*最后更新：2026-08-04*
