# 移动端自动化测试框架 — 工作总结

> 项目：低空空管自动化系统  
> 阶段：第一迭代 — 自动化测试框架建设（含第二阶段能力扩展）  
> 日期：2026-08-04（v2.0） / 2026-08-07（v3.0 能力扩展）  
> 版本：v3.0

---

## 一、项目背景

低空空管自动化系统正在从 PC Web 端向移动端扩展。移动端第一迭代包含五大模块：登录、首页（消息通知 + AI 问答）、申报（地图 + GPS + 飞行计划）、资讯、我的。当前阶段尚无正式 APK，采用手机 Chrome 浏览器访问 H5 页面进行测试。

---

## 二、完成工作

### 2.1 技术选型

| 技术 | 选择 | 原因 |
|------|------|------|
| 编程语言 | Python 3.13 | 生态丰富、团队熟悉 |
| 自动化引擎 | Appium 3.6.0 + UiAutomator2 8.2.2 | 业界标准、支持 native/browser 双模式 |
| 测试框架 | Pytest 9.1.1 | 插件体系完善、参数化能力强 |
| 报告 | Allure 2.16.0 | 可视化报告、CI/CD 集成 |
| 配置 | PyYAML 6.0 | 可读性好、支持多环境切换 |
| 设计模式 | Page Object Model | 可维护性高、页面变更影响小 |
| 浏览器驱动 | ChromeDriver 150.0.7871 | 匹配手机 Chrome 版本 |

### 2.2 交付成果

| 类别 | 数量 | 说明 |
|------|------|------|
| Python 源文件 | 18 个 | 配置/驱动/页面/测试/工具 |
| YAML 配置文件 | 4 个 | 主配置 + 测试数据 × 3 |
| 脚本文件 | 4 个 | run_tests.py + start.bat + start.sh + setup_check.py |
| 交互式模块 | 1 个 | manual_test.py（支持 browser/native 双模式） |
| 文档 | 5 个 | README + 测试方案 + 工作日志 + 工作总结 + 系统架构 |
| 虚拟环境 | 1 个 | .venv（33 个依赖包） |
| 测试用例 | 36 个 | 五大模块全覆盖，6 个标记分类 |

### 2.3 核心架构特性

1. **双模式支持**：配置文件 `test_mode` 一键切换 browser 模式（H5 测试）和 native 模式（APK 测试）
2. **Page Object Model**：四层架构（测试用例 → Page Object → Driver → 基础设施），页面变更只需修改 Page 类
3. **自动设备适配**：通过 ADB 自动检测设备 Android 版本，修正配置中的版本号
4. **失败自动诊断**：失败截图 + 页面源码自动附加到 Allure 报告
5. **环境自检**：`setup_check.py` 一键检查 10 项环境前提条件

---

## 三、解决的关键技术问题

### 3.1 Appium 3.6 + Android 16 + Chrome 150 兼容链

这是本次工作中遇到的最大挑战。三个组件都是较新版本，存在多个兼容性问题：

```
Appium 3.6.0
    ├── ANDROID_HOME 环境变量 → 设置系统环境变量 + 代码自动检测
    ├── platformVersion 写死 → ADB 自动检测修正
    ├── hidden_api_policy 权限 → --relaxed-security 绕过
    ├── language/locale 拼接错误 → 移除配置项
    └── --allow-insecure 格式变更 → 改用 --relaxed-security

UiAutomator2 8.2.2
    └── skipDeviceInitialization 不生效 → set_capability 直接注入

Chrome 150.0.7871
    └── ChromeDriver 映射表缺失 → 手动下载 150.0.7871.124
```

### 3.2 浏览器模式 vs 原生模式的架构设计

两个模式共用同一套 Page Object 接口，底层由 `AppiumDriverManager` 根据 `test_mode` 配置自动选择：

- **browser 模式**：`browserName: Chrome` + ChromeDriver → CSS Selector / XPath 定位
- **native 模式**：`appPackage` + `appActivity` + UiAutomator2 → resource-id / UiSelector 定位

---

## 四、测试覆盖情况

```
模块      用例数  smoke  正常流程  异常场景  边界  权限  离线
──────────────────────────────────────────────────────────
登录        8      1       1        5       1     0     0
首页        8      2       6        0       0     0     0
申报        8      0       4        1       0     2     1
资讯        5      1       4        0       0     0     0
我的        7      1       6        1       0     0     0
──────────────────────────────────────────────────────────
合计       36      5      21        7       1     2     1
```

---

## 五、后续工作建议

### 短期（APK 就绪后）

- 获取实际 APK 的 resource-id，更新 Page Object 定位器
- 在真实设备上跑通全部 36 个用例
- 浏览器模式下确认测试 URL 可访问后，验证登录/首页等核心流程

### 中期（迭代开发中）

- CI/CD 集成（Jenkins pipeline + 每日构建）
- 多设备并行执行（pytest-xdist）
- 数据工厂（批量生成测试数据）
- 性能基线建立

### 长期（多端扩展）

- iOS 适配（XCUITest 驱动）
- 跨端框架适配（根据 Flutter/RN 选型调整）
- 鸿蒙自动化方案评估

---

## 六、经验总结

### 做得好的

1. **文档先行**：编码前充分分析 13 个 HTML 原型页面，理解完整交互流程
2. **分层清晰**：四层架构职责明确，Page Object 定位器双备份（resource-id + UiSelector）
3. **容错设计**：每个操作含异常捕获和日志，失败信息完整可追溯
4. **环境自检**：setup_check.py 让新设备部署从"半天排查"变成"一键检测"
5. **渐进式调试**：遇到兼容性问题逐个击破，8 个问题全部定位根因并修复

### 可改进的

1. **Mock 服务**：当前依赖真实后端，后续需加入接口 Mock
2. **测试数据**：当前静态 YAML，后续可引入 Faker 动态生成
3. **执行速度**：浏览器模式下每个 Session 启动约 10 秒，可考虑 Session 复用

---

## 七、第二阶段：框架能力扩展（2026-08-07）

### 7.1 扩展内容

在原型框架（browser 模式测 H5）基础上，为正式 APK 测试扩展 6 大能力，
**6 项需求全部可在 Python + Appium 框架中实现**：

| 能力 | 实现 | 标记 |
|------|------|------|
| APK 管理 | 本地版本解析（aapt 三级回退）+ 自动对比安装/升级/卸载 | `install` |
| App 生命周期 | 冷/热启动耗时（am start -W）、关闭、后台/回前台 | `lifecycle` |
| 登录注册 | 登录已有；注册预留（定位器待校准自动激活） | `register` |
| 截图/日志 | 截图已有；logcat 抓取 + ANR/崩溃分析 + 失败 hook 附带现场 | — |
| 性能 | CPU/内存/电量/启动耗时/响应时间，warn_only 阈值先建基线 | `perf` |
| 异常 | 开关级断网/飞行模式 + 限速接口预留 + monkey + ANR 检测 | `weaknet`/`stability` |

### 7.2 新增交付成果

| 类别 | 数量 | 说明 |
|------|------|------|
| 工具模块 | 5 个 | apk_manager / app_lifecycle / performance / logcat / network_controller |
| ADB 新方法 | 8 个 | 启动耗时/进程状态/性能快照/monkey |
| 测试文件 | 6 个 | 45 个新用例（总计 88 个用例，11 个标记） |
| 页面对象 | 1 个 | register_page.py（预留） |
| 数据文件 | 1 个 | register_data.yaml（预留） |
| 配置段 | 4 个 | apk / performance / network / stability |
| 文档 | 3 篇 | APK 管理 / 性能基线 / 弱网方案 |

### 7.3 关键架构决策

1. **ADB 层核心**：性能/生命周期/APK 能力全部走 ADB，不依赖 driver —— 双模式兼容、
   driver 挂掉后仍能采集
2. **守卫机制**：`require_apk`/`require_native_mode` 用 skip 而非 fail ——
   APK 未就绪时仓库保持全绿
3. **弱网分层**：开关级断网已实现；限速通过 `ThrottleController` 协议预留
   （真机弱网限速必须 PC 侧代理，接入步骤已写入文档）
4. **性能基线流程**：warn_only 默认不失败 → `--perf-baseline` 跑 3 次取中位
   → 回填 config 阈值 → `--perf-strict` 严格回归

### 7.4 下一步（正式 APK 就绪后）

1. APK 放入 `apk/` 目录 → `--module install` 自动安装/升级
2. `test_mode` 切换 native，Appium Inspector 校准定位器
3. 建立性能基线（冷启动/热启动/内存），回填阈值后启用严格模式
4. 注册页定位器校准，注册用例自动激活
5. CI/CD 集成（Jenkins）

---

*文档版本：v3.0*  
*编写日期：2026-08-07*
